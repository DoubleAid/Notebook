# 碰撞检测

判断两个物体是否发生了空间上的重叠，最直接的理解就是判断两个点之间或以点为中心的简单几何体（如球体）是否发生碰撞

## 情况一：点与点的碰撞

计算 两点之间的距离并判断距离是否超过阈值

## 情况二：球体与球体的碰撞

这是游戏和图形学中最常见、最高效的3D碰撞检测方法之一。我们将每个物体视为被一个球体（包围球）包裹。

## 情况三：四叉树/八叉树

递归地将一个二维平面划分为四个象限（区域）。通过两个物体的四叉树占用标记检测是否发生碰撞