# 关键帧选取条件

## 核心运动约束（和激光一致）

+ 平移距离：当前帧和上一关键帧的平移量查过阈值
+ 旋转角度：旋转角度超过阈值，保证视角多样性
+ 时间间隔保底：设置最小时间间隔，防止静止/低速场景无关键帧

## 视觉特征专属条件

+ 特征点数量：当前帧提取的有效特征点（如ORB角点）数量需要大于阈值，否则舍弃
+ 视差约束：当前帧和上一关键帧的特征点视差需适中（避免过小导致三角化精度第，过大导致特征匹配失效）
+ 特征匹配率：当前帧和局部地图的特征匹配率高于阈值，保证与地图的关联性

## 补充条件

+ 场景变化检测：若环境特征分布突变（如进入新场景），强制插入关键帧
